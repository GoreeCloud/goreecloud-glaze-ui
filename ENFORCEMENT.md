# GLAZE UI V1.3 — Enforcement

The current Glaze UI enforcement and consumer-conformance target is **GLAZE UI V1.3 — Adaptive Resonance** (`1.3.0`). `registry/lifecycle.json` and `VERSION` are the current lifecycle authorities. GLAZE UI V1.2 — Living Frosted (`1.2.0`) remains the previous known-good Stable rollback and audit baseline and does not override the current target.

Enforcement fails closed when required V1.3 evidence is absent, stale, or bound to a different revision. Consumers must not claim conformance from copied tokens, renamed assets, screenshots alone, a platform declaration, historical V1.2 evidence, or the shared V1.3 Stable promotion itself. Required checks include exact-revision contract validation, accessibility, supported form factors, rendered/native evidence where applicable, performance budgets, and product-specific production acceptance.

The V1.0 reset established the V1 namespace and remains historical baseline evidence. V1.1 and V1.2 Stable records and Candidate source lineages remain preserved for rollback, audit, and promoted-source provenance. V1.3.0 is the live Stable authority with no active Candidate. The six unresolved human/manual/physical/native-adapter/source-namespace qualification tracks are explicitly transferred to V1.3.1 and are not represented as passed V1.3.0 evidence.
