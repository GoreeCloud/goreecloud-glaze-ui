# GLAZE UI V1.1.1 Patch Candidate — Import Closure

## Status

**Lifecycle:** Release Candidate  
**Candidate:** `1.1.1-rc.1`  
**Base Stable:** GLAZE UI V1.1 / `1.1.0`  
**Base Stable revision:** `15cc76d2bcd4065552dc31c77145b63f34d9e7b2`  
**Base immutable tag:** `v1.1.0`

Current Stable and current official authority remain `1.1.0`. This record identifies the corrective `1.1.1-rc.1` candidate; it does not alter the immutable `v1.1.0` release or establish `1.1.1` as Stable.

## Defect

The Stable V1 web graph reaches `css/glaze-v1.components.css` through `css/glaze-v1.0.0.css`. The component layer contained a stale import of `./glaze-v1.candidate.css`, but that path was never committed in the V1 namespace. The reset already carried the required foundation forward as `css/glaze-v1.foundation.css`, and the official V1 entrypoint loads that foundation before the component layer.

Strict CSS processors and asset pipelines therefore failed while resolving the published Stable graph even though earlier source-level release checks did not traverse import closure.

## Candidate repair

The candidate:

- removes only the stale Candidate dependency from the inherited V1 component layer;
- does not recreate a competing Candidate foundation;
- preserves the explicit `glaze-v1.foundation.css` load order in the official V1 entrypoint;
- adds recursive import-closure validation to the normal `scripts/validate_glaze_v1.py` path;
- rejects missing imports, remote imports, root escapes, query/fragment imports, symlinked targets, and import cycles;
- self-tests negative import cases before validating the repository graph.

## Qualification checkpoint

Repaired source revision `874f8542ba60d37d2b847fd547a0b976e788bbab` passed all six pull-request gate families before the lifecycle record advanced to Release Candidate:

- GLAZE UI V1.1 CI — run `33887608495` — passed.
- Glaze V1.1 Candidate — run `33887608461` — passed.
- Glaze V1.1 Release Evidence — run `33887608407` — passed.
- Glaze UI Semantic Color Contract — passed.
- GLAZE UI V1 Icon Construction Contract — passed.
- Glaze UI Icon Identity Contract — passed.

The Release Evidence run produced source-pinned web artifact `9942607687` with digest `sha256:3bd135b77cce0379e58b5b3c3eb80b0c0ff63d0ab786edd056f46039fb66a12e` and Android native artifact `9942712257` with digest `sha256:a4e73879871369340c881f4694a503c28f32ec063d1f03549c64cdef9fff019c`.

This checkpoint is preserved as historical qualification evidence for that exact revision. Because recording Release Candidate lifecycle metadata changes the exact pull-request head, the updated head must repeat all applicable exact-head gates before merge.

## Rendered-evidence repeatability remediation

On exact Release Candidate head `efe0d0a627de001c3f8f46055e4cde9f391b825d`, Release Evidence run `33888711509` initially rendered all web scenes successfully but failed the approved mobile PNG hash: expected `d8001c4a3706145a78c6c9638288ba35339e777316b9b7e5ca4aec3aef2fe98f`, observed `1d3d8dcab16db1f22fb55bdaac8984ba0a0eda4ec67cd89b37857a06ece67c58`. The Android job on the same exact head passed.

A source comparison from previously green head `874f8542ba60d37d2b847fd547a0b976e788bbab` to `efe0d0a627de001c3f8f46055e4cde9f391b825d` showed changes only to acceptance, candidate-contract, lifecycle, and candidate-validator records; no CSS, HTML reference scene, token, or renderer source changed. Both web runs used the same Ubuntu runner image. Re-running only the failed web job on the same exact head then reproduced the human-approved baseline successfully. This establishes a capture-repeatability defect rather than an authorized optical change and does not authorize replacement visual baselines.

Renderer remediation commit `84afde9a331a0ae46e1bbe3f82beafcae9dd1c59` adds fail-closed capture settling: it waits for document/font readiness and three animation frames, normalizes scroll/layout state, captures each screenshot twice, and rejects the evidence if the two PNG byte streams differ. The temporary maintenance workflow self-removed after applying the bounded source change.

Because the renderer itself changed, the current exact head must now pass all applicable pull-request gates again. A prior green retry is diagnostic evidence only and is not substituted for fresh post-remediation acceptance.

## Remaining acceptance boundary

Before a `1.1.1` Stable release can be claimed, the exact Release Candidate head must pass fresh source/build/rendered/native acceptance, then complete governed merge, independent post-merge main validation, release preparation against the selected merged revision, a new immutable `v1.1.1` tag and GitHub Release when Stable qualification is actually satisfied, and canonical GoreeCloud documentation synchronization.

Because the dependency bytes change, downstream consumers must re-pin to the corrected immutable release and repeat applicable consumer-specific validation. Prior `1.1.0` consumer evidence remains historical evidence and is not silently rebound to the patch.

No downstream application becomes conformant, production-eligible, or Stable merely because this patch candidate passes Glaze repository CI.
